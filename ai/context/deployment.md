# rateboard — live deployment manifest

> GENERATED by the docs flow on 2026-09-23T23:25:31Z; re-run it to refresh (idempotent;
> command in [operations.md](operations.md)). Hand-edits here are
> overwritten. No secret values appear in this file — inventories are
> names only.

## Identity

| | |
|---|---|
| product / project slug | `rateboard` |
| Orun workspace | `ws_TZ1549ZN` (slug `rateboard`) |
| declared in | `intent.yaml` → `execution.state.workspace` / `.project` |
| CI lane CLI pin | orun `v2.56.2` (`.github/workflows/ci.yml`) |

## Public URLs (probed 2026-09-23T23:25:31Z)

| surface | URL | status |
|---|---|---|
| edge API (stage) | https://rateboard-api-edge-stage.nexo-7be.workers.dev/health | LIVE (200) |
| edge API (prod) | https://rateboard-api-edge-prod.nexo-7be.workers.dev/health | LIVE (200) |
| console (stage) | https://rateboard-web-console-next-stage.nexo-7be.workers.dev | LIVE (200) |
| console (prod) | https://rateboard-web-console-next-prod.nexo-7be.workers.dev | LIVE (200) |
| edge (custom domain) | https://api.rateboard.app/health | pending (phase 07 not run, or DNS propagating) |
| console (custom domain) | https://app.rateboard.app | pending (phase 07 not run, or DNS propagating) |

A 401/403 on `/health` still counts as deployed (alive but unauthorized);
000/5xx does not. The `dev` environment is verify-only by design — no
deployed URLs.

## Environments

- `stage` and `prod` are fully provisioned (D1 database, idempotency KV,
  migrations applied) and serve the URLs above.
- `dev` is verify-only: plan/verify lanes run, nothing is deployed.

## Secrets inventory (names only)

Project rung:
  - (listing unavailable to this credential)

Environment `stage`:
  - WIRING_CLOUDFLARE_D1
  - WIRING_CLOUDFLARE_KV

Environment `prod`:
  - WIRING_CLOUDFLARE_D1
  - WIRING_CLOUDFLARE_KV

Provider secrets are BROKERED (minted per-run from the workspace's
integration connections; no long-lived values at rest). `WIRING_*` env-rung
entries are job-output secrets published by the infrastructure terraform —
deploy lanes resolve them for worker bindings.

## Workspace integrations

  - cloudflare: active
  - github: active

## How this deployment changes

Merges to `main` converge automatically: CI plans changed components and
runs each lane via `orun run --remote-state` against the workspace above.
See [operations.md](operations.md) for the full operating contract
(convergence, resumes, secrets, verification, troubleshooting).
